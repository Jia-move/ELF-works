# ============================================================================
# RK3588 智能导览眼镜 — Python 依赖
# ============================================================================
# 安装：pip install -r requirements.txt
# 注意：rknn-toolkit-lite2 需要在 RK3588 设备上安装，非标准 PyPI 包
# ============================================================================

# 核心运行时依赖
opencv-python==4.10.0.84
numpy==2.2.1
rknn-toolkit-lite2==2.3.2
PyYAML==5.4.1

# 云端智能体（预留）
requests==2.32.3

# 语音播报（Mock 模式，可选 TTS 预留）
pygame==2.1.2

# 讯飞实时语音转写大模型 ASR（WebSocket）
websocket-client>=1.6.0

# Qt 可视化界面（PySide6 已安装并接入）
PySide6

# ============================================================================
# 后续功能所需依赖（当前未安装，按需添加）
# ============================================================================
# gtts 或 pyttsx3           — TTS 语音合成
# flask 或 fastapi           — Web 服务（云端下单）
# torch (仅用于模型训练，推理使用 RKNN/NPU)
